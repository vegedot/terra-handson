# =============================================================================
# Step 3: S3バックエンド設定（コメントアウト状態）
# =============================================================================
# この設定を有効化するには:
#   1. まず main.tf の terraform apply でS3バケットとDynamoDBテーブルを作成
#   2. 下記のコメントを外す
#   3. terraform init を再実行（ステートの移行が行われる）
#
# 注意:
#   - バケット名とテーブル名は main.tf で作成したものと一致させること
#   - リージョンも一致させること
# =============================================================================

# terraform {
#   backend "s3" {
#     # ステートファイルを保存するバケット名
#     bucket = "handson-dev-terraform-state"
#
#     # ステートファイルのパス（バケット内のキー）
#     key = "step03/terraform.tfstate"
#
#     # バケットのリージョン
#     region = "ap-northeast-1"
#
#     # ステートロック用のDynamoDBテーブル名
#     dynamodb_table = "handson-dev-terraform-lock"
#
#     # ステートファイルの暗号化
#     encrypt = true
#   }
# }
